import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      screens: {
        xxs: '380px',
        xs: '480px',
      },
    },
  },
};

export default config;
